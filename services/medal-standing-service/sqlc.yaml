version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/query.sql"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
